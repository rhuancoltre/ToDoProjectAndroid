<?xml version="1.0" encoding="utf-8"?>
<resources>

    <string name="createTodo">
        CREATE TABLE IF NOT EXISTS ToDo
        (
            id INTEGER primary key AUTOINCREMENT,
            descricao TEXT,
            dataEntrega TEXT,
            prioridade NUMERIC,
            categoria INTEGER
        )
    </string>

    <string name="getTodos">
        SELECT * FROM ToDo
    </string>

    <string name="updateTodo">
        UPDATE TABLE ToDo
        (
            descricao TEXT,
            dataEntrega TEXT,
            prioridade NUMERIC,
            categoria INTEGER
        )
    </string>

</resources>
